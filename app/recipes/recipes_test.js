'use strict';

describe('recipes.home module', function() {

  beforeEach(module('recipes.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('Home Ctrl');
      expect(homeCtrl).toBeDefined();
    }));

  });
});